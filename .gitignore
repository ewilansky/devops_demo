# Compiled class file
*.class

### STS ###
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache

# Log file
*.log

# BlueJ files
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# Package Files #
# *.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*

# added for devops demo project
upd.sh

## file system files
*.lock
.DS_Store

## pattern to exclude everything in the build directory except for the jar
## eventually the jar will come from another repository, like Nexus
!spring-boot-demo/

# ignore everything at the root
# spring-boot-demo/*
# but don't ignore this file
!spring_boot_demo/gradle.properties
# but don't ignore this directory

!spring-boot-demo/build/

# ignore this directory
spring-boot-demo/build/*
# but don't ignore this subdir
!spring-boot-demo/build/libs/

spring-boot-demo/bin/
spring-boot-demo/build-cache/
spring-boot-demo/gradle/
spring-boot-demo/target/



# Not required for Gradle container-based build ###
/gradle
gradleew.*
.gradle

# Removing from SCM, this was an altenrative direction for jenkins, but decided not to build a custom image for jenkins
jenkins_build_def/*
# docker-compose converted by kompose yaml files for kunbernetes deployments. These aren't being used, but useful local reference only

# docker-compose converted by kompose yaml files for kunbernetes deployments. These aren't being used, but useful local reference only
kube_yamls/converted_infrastructure/*

# the following directories are automatically created when running docker-compose to generate
# containers from these images
# nexus_home/*
# postgres_home/*
# sonar_home/*
